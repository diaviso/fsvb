<form method="post" action="{{ path('app_affiliation_delete', {'id': affiliation.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ affiliation.id) }}">
    <button class="btn">Delete</button>
</form>
